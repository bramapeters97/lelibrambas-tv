// Copy to Secrets.xcconfig locally, then replace only on an authorized build host.
// These reserved example.test values are deliberately non-routable placeholders.
API_BASE_URL = https:/$()/gateway.example.test
ACTIVATION_BASE_URL = https:/$()/activate.example.test
